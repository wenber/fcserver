/**
 * @file 默认配置
 * @author wenber
 */

/* global process */

'use strict';

module.exports = {
    // 默认服务器配置文件名
    defaultConf: 'fcserver-config.js',
    // 支持实时更新的文件类型
    supportUpdateType: /\.(js|html|css|less)$/,
};
