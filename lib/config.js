/**
 * @file 默认配置
 * @author wenber
 */

'use strict';

module.exports = {
    // 默认端口
    defaultPort: 8080,
    // 默认文件名
    defaultConf: 'fcserver-config.js',
    // 路由映射到处理器
    path2Handler: []
};
